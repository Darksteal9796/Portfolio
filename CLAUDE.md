# CLAUDE.md — Project Config

> Project-specific instructions for Claude Code. Lives in the project root.
> Global rules are in ~/.claude/CLAUDE.md.

## Project

**Name:** <project-name>
**What it is:** <1–2 sentences>
**Audience:** <who uses it>
**Status:** <pre-alpha | alpha | beta | production>

## Source of truth

- **Spec:** docs/spec.docx (or link)
- **Brief:** docs/brief.md
- **Architecture:** docs/RECON_REPORT.md (generated by /recon)

## Tech stack

- **Language:** <e.g. Python 3.12>
- **Framework:** <e.g. FastAPI 0.115>
- **Package manager:** <uv | pip | pnpm>
- **Tests:** <pytest | vitest | playwright>
- **Lint/format:** <ruff + black | eslint + prettier>
- **Database:** <postgres 16 | sqlite | none>
- **Deploy:** <AWS Lambda | Vercel | Cloudflare Pages | Docker on EC2>

## Build & run

```
<install command>
<dev command>
<test command>
<lint command>
<build command>
```

## Conventions specific to this project

- <naming / layout / patterns>
- <error handling rules>
- <how to add a new endpoint / component / migration>

## AI-specific (if the project uses LLMs)

- **Model(s):** <OpenAI GPT-4.1 | Claude Sonnet 4.6 | etc>
- **Providers fallback order:** <primary → fallback>
- **Prompt location:** prompts/ directory, versioned
- **Eval dataset:** docs/evals/<dataset>
- **Cost budget:** <$X/month | per-user cap>

## Critical rules

- <project-specific "do not" list>
- Example: "Never push directly to main — PR only."
- Example: "All model calls must go through lib/llm/ — no direct SDK calls elsewhere."

## Standing checks before commit

- [ ] Tests green
- [ ] Lint clean
- [ ] /voice-check on any user-facing copy
- [ ] /secure on anything touching auth, payments, user data
- [ ] /ai-review on anything touching LLMs or RAG
